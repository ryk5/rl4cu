# together.ai key + base url — used for baseline eval (Qwen2.5-Coder-32B)
OPENAI_API_KEY=your_together_ai_key_here
OPENAI_BASE_URL=https://api.together.xyz/v1

# openai key — used for trajectory generation (gpt-4o teacher)
# the generate_trajectories.py script reads this first if set
OPENAI_API_KEY_OPENAI=sk-...
